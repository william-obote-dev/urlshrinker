import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "URLShrinker — High-Throughput URL Shortener",
  description:
    "Production-grade URL shortener with Redis caching, sub-5ms latency, " +
    "real-time analytics, and QR code generation. Built by William Obote.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className="min-h-screen bg-navy antialiased">{children}</body>
    </html>
  );
}
